{
  "name": "@btc/bi",
  "version": "1.0.0",
  "description": "BTC MES BI系统",
  "type": "module",
  "scripts": {
    "dev": "echo 'BI system dev server'",
    "build": "if not exist dist mkdir dist && echo BI system build > dist/build.txt",
    "lint": "echo 'BI system lint'",
    "typecheck": "echo 'BI system typecheck'",
    "clean": "if exist dist rmdir /s /q dist"
  }
}
