{
  "name": "@btc/styles",
  "version": "1.0.0",
  "description": "BTC SaaS 统一样式系统",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src/**/*"
  ],
  "scripts": {
    "build": "node build.js",
    "dev": "sass src/index.scss dist/index.css --watch --quiet --no-source-map",
    "lint": "stylelint src/**/*.scss"
  },
  "devDependencies": {
    "sass": "^1.69.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard-scss": "^13.0.0"
  },
  "peerDependencies": {
    "element-plus": "^2.11.0"
  }
}
